def insert_at_start(self, data):
    temp = Node(data)
    temp.next = self.head
    self.head= temp
def insert_at_end(self, data):
    temp = Node(data)
    if self.head = is None
        self.head = temp
        return
    n = self.head
    while n.next is not None:
        n = n.next
    n.next = temp;
def insert_after_data()
    